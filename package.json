{
  "name": "ipfs-london-intro-talk",
  "version": "1.0.0",
  "private": true,
  "description": "Welcome the people, introduce the talks and thank the sponsors",
  "main": "dist/index.html",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "test": "standard",
    "deploy": "npm run build; npm run ipfs",
    "ipfs": "ipfs add -r -Q dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.9.3",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "highlight.js": "^9.12.0",
    "ipfs-css": "^0.5.2",
    "tachyons": "^4.10.0"
  }
}
